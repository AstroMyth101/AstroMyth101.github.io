## 2024-07-08 - Added AVIF support to Eleventy Image Shortcode
**Learning:** Adding the `avif` format to the `@11ty/eleventy-img` configuration can significantly reduce frontend payload sizes with minimal changes, as it falls back to `webp` gracefully.
**Action:** Always check the image optimization pipeline for next-generation formats like AVIF to achieve quick performance wins.
